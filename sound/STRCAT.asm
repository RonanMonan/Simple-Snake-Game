STRCAT PROC
	PUSH CX
	PUSH DX
	
	MOV CX, [SI]
FIND_END:
	MOV AL, '$'
	JE START_COPY
	DEC CX
	JMP FIND_END
	
START_COPY:
	LEA DX, [SI+CX]
COPY_LOOP:
	LODSB
	STOSB
	CMP AL, '$'
	JNZ COPY_LOOP
	
	POP DX
	POP CX
	RET
STRCAT ENDP